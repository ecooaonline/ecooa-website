---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroEditorial from '../components/HeroEditorial.astro';
import ProfessionalCard from '../components/ProfessionalCard.astro';
import CtaSection from '../components/CtaSection.astro';
import { WA_DEFAULT } from '../data/constants';
import { professionals } from '../data/professionals';

const WA = WA_DEFAULT;

const SITE_URL = 'https://www.somosecooa.com.br';

// Ordem editorial dos cards por seção (definida pela gestão da clínica).
// Para reordenar, mude apenas estas listas; os dados dos cards (nome, cargo,
// registro, foto, tags) vêm de professionals.ts.
const MED_ORDER = [
  'gustavo-gehrke',
  'yale-jeronimo',
  'larissa-wiebbelling',
  'vitoria-machado',
  'renata-bohn',
];
const ESTHETIC_ORDER_A = [
  'danusa-pires',
  'viviane-fagundes',
  'natalie-queiroz',
  'leticia-de-melo',
  'susan-flach',
  'jennifer-adam',
];
// Eduarda e Tais (cards sem foto, aguardando material) entram entre A e B.
const ESTHETIC_ORDER_B = ['karine-ellwanger', 'jamylle-farias', 'adriana'];
const WORKING_ORDER = [
  'jessica-stein',
  'maria-luisa-beltran',
  'adriano-lenz',
  'giancarla-rochemback',
  'vitoria-serpa',
  'lara-caye',
  'daniel-forster',
  'camila-cadore',
  'marvin-marques',
  'gabrieli-klagenberg',
  'nasser-salem',
  'verena-cattani',
];
const MIND_ORDER = ['manuela-vanti', 'augusto-kauer', 'francielle-beria'];
const CARD_BG = 'linear-gradient(145deg,#E8E0D0,#D4C9B0)';

function inOrder(slugs: string[]) {
  return slugs
    .map((slug) => professionals.find((p) => p.slug === slug))
    .filter((p): p is (typeof professionals)[number] => Boolean(p));
}

function isPhysician(role: string) {
  return /\bMédic[oa]\b/i.test(role);
}

function medicalSpecialty(role: string): string | null {
  const lower = role.toLowerCase();
  if (lower.includes('dermato') || lower.includes('capilar') || lower.includes('tricolog'))
    return 'Dermatology';
  if (lower.includes('psiquiatr')) return 'Psychiatric';
  if (lower.includes('cardiolog')) return 'Cardiovascular';
  if (lower.includes('endocrin')) return 'Endocrine';
  if (
    lower.includes('ortomolecular') ||
    lower.includes('metabol') ||
    lower.includes('hormôn') ||
    lower.includes('longevidade') ||
    lower.includes('emagrecimento')
  )
    return 'PrimaryCare';
  return 'PrimaryCare';
}

const schemaMembers = professionals.map((p) => {
  const base = {
    name: p.name,
    description: p.description,
    worksFor: { '@type': 'MedicalBusiness', name: 'ecooa' },
    image: `${SITE_URL}${p.photo}`,
    url: `${SITE_URL}/profissionais/${p.slug}`,
  };
  if (isPhysician(p.role)) {
    return { '@type': 'Physician', ...base, medicalSpecialty: medicalSpecialty(p.role) };
  }
  return { '@type': 'Person', ...base, jobTitle: p.role.split('·')[0].trim() };
});
---

<BaseLayout
  title="Profissionais"
  description="Especialistas da ecooa. Médicos, nutricionistas, psicólogos e profissionais de estética em Moinhos de Vento, Porto Alegre."
  currentPage="profissionais"
  ogImage="/clinic/reuniao-edit.webp"
  breadcrumbs={[{ name: 'Profissionais', url: '/profissionais' }]}
>
  <HeroEditorial
    label="corpo clínico"
    title="Especialistas que<br /><em>definem padrões.</em>"
    sub="Uma equipe multidisciplinar reunida em torno de um propósito: oferecer o melhor cuidado possível, com humanidade e excelência técnica."
    ctaHref={WA}
    ctaLabel="falar pelo whatsapp"
    ctaIntent="profissionais-hero"
    secondaryHref="/match"
    secondaryLabel="não sabe com quem agendar? ecooa.match"
    image="/clinic/reuniao-edit.webp"
    imageAlt="Equipe multidisciplinar da ecooa reunida na sala de reuniões"
    imageCaption="a casa reunida · moinhos de vento"
  />

  <!-- Professionals Structured Data -->
  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'MedicalOrganization',
      name: 'ecooa',
      url: `${SITE_URL}/profissionais`,
      member: schemaMembers,
    })}
  />

  <!-- Corpo clínico -->
  <section
    style="background:var(--color-warm);padding:88px var(--section-px) 112px"
    aria-label="Equipe de profissionais"
  >
    <div style="max-width:1280px;margin:0 auto">
      <!-- Sub-navegação por área -->
      <nav
        aria-label="Navegar por área"
        class="r"
        style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:96px"
      >
        <a href="#medicina" class="tag-pill" style="text-decoration:none">medicina</a>
        <a href="#estetica" class="tag-pill" style="text-decoration:none">estética</a>
        <a href="#nutricao" class="tag-pill" style="text-decoration:none">nutrição</a>
        <a href="#saude-mental" class="tag-pill" style="text-decoration:none">saúde mental</a>
      </nav>

      <!-- Medicina -->
      <div class="lbl r" id="medicina" style="margin-bottom:14px;scroll-margin-top:110px">
        ecooa.med
      </div>
      <h2 class="h2 r d1" style="margin-bottom:16px">Medicina</h2>
      <p class="body r d2" style="max-width:520px;margin-bottom:30px">
        Longevidade com precisão: metabolismo, hormônios, pele e cabelo investigados com
        profundidade.
      </p>
      <div class="r d2" style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:48px">
        <span class="tag-pill">Medicina Metabólica</span>
        <span class="tag-pill">Dermatologia Clínica</span>
        <span class="tag-pill">Dermatologia Estética</span>
        <span class="tag-pill">Tricologia Médica</span>
        <span class="tag-pill">Transplante Capilar</span>
      </div>
      <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(225px,1fr));gap:18px">
        {
          inOrder(MED_ORDER).map((p) => (
            <ProfessionalCard
              clickable
              slug={p.slug}
              name={p.name}
              role={p.role}
              bg={CARD_BG}
              ig={p.ig}
              photo={p.photo}
              description={p.description}
              tags={p.tags}
            />
          ))
        }
      </div>

      <div class="div" style="margin:96px 0"></div>

      <!-- Estética -->
      <div class="lbl r" id="estetica" style="margin-bottom:14px;scroll-margin-top:110px">
        ecooa.esthetic
      </div>
      <h2 class="h2 r d1" style="margin-bottom:16px">Estética</h2>
      <p class="body r d2" style="max-width:520px;margin-bottom:30px">
        Beleza que respeita a sua essência, com técnica e critério.
      </p>
      <div class="r d2" style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:48px">
        <span class="tag-pill">Tricologia</span>
        <span class="tag-pill">Biomedicina Estética</span>
        <span class="tag-pill">Harmonização Orofacial</span>
        <span class="tag-pill">Osteopatia</span>
      </div>
      <div class="g4">
        {
          inOrder(ESTHETIC_ORDER_A).map((p) => (
            <ProfessionalCard
              clickable
              slug={p.slug}
              name={p.name}
              role={p.role}
              bg={CARD_BG}
              ig={p.ig}
              photo={p.photo}
              description={p.description}
              tags={p.tags}
            />
          ))
        }
        <ProfessionalCard
          name="Eduarda Schoenmeier"
          photo="/team/placeholder.webp"
          role="Biomédica Esteta · Harmonização Facial e Corporal"
          bg={CARD_BG}
          registration="CRBM 7243"
        />
        <ProfessionalCard
          name="Tais de la Rosa"
          photo="/team/placeholder.webp"
          role="Farmacêutica · Skin Expert"
          bg={CARD_BG}
          registration="CRF-RS 588527"
        />

        {
          inOrder(ESTHETIC_ORDER_B).map((p) => (
            <ProfessionalCard
              clickable
              slug={p.slug}
              name={p.name}
              role={p.role}
              bg={CARD_BG}
              ig={p.ig}
              photo={p.photo}
              description={p.description}
              tags={p.tags}
            />
          ))
        }
      </div>

      <div class="div" style="margin:96px 0"></div>

      <!-- Nutrição -->
      <div class="lbl r" id="nutricao" style="margin-bottom:14px;scroll-margin-top:110px">
        ecooa.working
      </div>
      <h2 class="h2 r d1" style="margin-bottom:16px">Nutrição especializada</h2>
      <p class="body r d2" style="max-width:520px;margin-bottom:48px">
        Alimentação que sustenta a sua melhor versão, da consulta clínica à alta performance.
      </p>
      <div class="g4">
        {
          inOrder(WORKING_ORDER).map((p) => (
            <ProfessionalCard
              clickable
              slug={p.slug}
              name={p.name}
              role={p.role}
              bg={CARD_BG}
              ig={p.ig}
              photo={p.photo}
              description={p.description}
              tags={p.tags}
            />
          ))
        }
      </div>

      <div class="div" style="margin:96px 0"></div>

      <!-- Saúde Mental -->
      <div class="lbl r" id="saude-mental" style="margin-bottom:14px;scroll-margin-top:110px">
        ecooa.mind
      </div>
      <h2 class="h2 r d1" style="margin-bottom:16px">Saúde mental</h2>
      <p class="body r d2" style="max-width:520px;margin-bottom:30px">
        Clareza mental para uma vida com propósito, com escuta profissional e acolhimento.
      </p>
      <div class="r d2" style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:48px">
        <span class="tag-pill">Psicologia</span>
        <span class="tag-pill">TCC</span>
        <span class="tag-pill">Psicologia Esportiva</span>
      </div>
      <div class="g4">
        {
          inOrder(MIND_ORDER).map((p) => (
            <ProfessionalCard
              clickable
              slug={p.slug}
              name={p.name}
              role={p.role}
              bg={CARD_BG}
              ig={p.ig}
              photo={p.photo}
              description={p.description}
              tags={p.tags}
            />
          ))
        }
      </div>
    </div>
  </section>

  <!-- Link discreto B2B -->
  <section
    style="background:var(--color-warm);padding:0 var(--section-px) 40px"
    aria-label="Para profissionais"
  >
    <p
      class="r"
      style="text-align:center;font-size:12px;font-weight:300;color:var(--color-ink-soft)"
    >
      É profissional de saúde? <a
        href="/para-profissionais"
        style="color:var(--color-brand-text);text-underline-offset:3px;text-decoration:underline"
        >Conheça a ecooa para profissionais →</a
      >
    </p>
  </section>

  <CtaSection
    heading="Conheça nossa<br><em>equipe pessoalmente.</em>"
    subtext="Agende sua avaliação e descubra qual profissional da ecooa é o ideal para o seu momento."
  />

  <!-- Modal overlay -->
  <div
    class="pro-modal-overlay"
    id="pro-modal-overlay"
    aria-hidden="true"
    role="dialog"
    aria-modal="true"
    aria-labelledby="pm-name"
  >
    <div class="pro-modal" id="pro-modal">
      <button class="pro-modal-close" id="pro-modal-close" aria-label="Fechar">✕</button>
      <img
        class="pro-modal-photo"
        id="pm-photo"
        src=""
        alt=""
        loading="lazy"
        decoding="async"
        width="480"
        height="640"
      />
      <div class="pro-modal-info">
        <p class="pro-modal-name" id="pm-name"></p>
        <p class="pro-modal-role" id="pm-role"></p>
        <p class="pro-modal-desc" id="pm-desc"></p>
        <div class="pro-modal-tags" id="pm-tags"></div>
        <div class="pro-modal-actions">
          <a id="pm-wa" href="#" target="_blank" rel="noopener noreferrer" class="bd"
            >agendar avaliação</a
          >
          <a
            id="pm-ig"
            href="#"
            target="_blank"
            rel="noopener noreferrer"
            class="ghost"
            style="justify-content:center"
          >
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" aria-hidden="true"
              ><rect
                x="2"
                y="2"
                width="20"
                height="20"
                rx="6"
                stroke="currentColor"
                stroke-width="1.5"></rect><circle
                cx="12"
                cy="12"
                r="4"
                stroke="currentColor"
                stroke-width="1.5"></circle><circle cx="17" cy="7" r=".8" fill="currentColor"
              ></circle></svg
            >
            <span id="pm-ig-label"></span>
          </a>
          <a id="pm-profile" href="#" class="ghost" style="justify-content:center;font-size:11px"
            >ver perfil completo →</a
          >
        </div>
      </div>
    </div>
  </div>

  <script>
    const overlay = document.getElementById('pro-modal-overlay')!;
    const modal = document.getElementById('pro-modal')!;
    const closeBtn = document.getElementById('pro-modal-close')!;
    const pmPhoto = document.getElementById('pm-photo') as HTMLImageElement;
    const pmName = document.getElementById('pm-name')!;
    const pmRole = document.getElementById('pm-role')!;
    const pmDesc = document.getElementById('pm-desc')!;
    const pmTags = document.getElementById('pm-tags')!;
    const pmWa = document.getElementById('pm-wa') as HTMLAnchorElement;
    const pmIg = document.getElementById('pm-ig') as HTMLAnchorElement;
    const pmIgLabel = document.getElementById('pm-ig-label')!;
    const pmProfile = document.getElementById('pm-profile') as HTMLAnchorElement;

    function openModal(card: HTMLElement) {
      const name = card.dataset.name || '';
      const role = card.dataset.role || '';
      const photo = card.dataset.photo || '';
      const ig = card.dataset.ig || '';
      const igUrl = card.dataset.igUrl || '';
      const wa = card.dataset.wa || '';
      const desc = card.dataset.desc || '';
      const tags = card.dataset.tags ? card.dataset.tags.split('|') : [];

      pmPhoto.src = photo;
      pmPhoto.alt = name;
      pmName.textContent = name;
      pmRole.textContent = role;
      pmDesc.textContent = desc;
      pmTags.textContent = '';
      tags.forEach((t) => {
        const span = document.createElement('span');
        span.className = 'pro-modal-tag';
        span.textContent = t;
        pmTags.appendChild(span);
      });
      pmWa.href = wa;
      pmWa.setAttribute('data-professional-name', name);
      if (igUrl) {
        pmIg.href = igUrl;
        pmIgLabel.textContent = ig;
        pmIg.style.display = '';
      } else {
        pmIg.style.display = 'none';
      }
      const slug = card.dataset.slug || '';
      if (slug) {
        pmProfile.href = `/profissionais/${slug}`;
        pmProfile.style.display = '';
      } else {
        pmProfile.style.display = 'none';
      }

      overlay.classList.add('active');
      overlay.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
      closeBtn.focus();
    }

    let lastOpenerCard: HTMLElement | null = null;

    function closeModal() {
      overlay.classList.remove('active');
      overlay.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
      lastOpenerCard?.focus?.();
      lastOpenerCard = null;
    }

    // Click on card
    document.querySelectorAll('[data-modal="professional"]').forEach((card) => {
      card.addEventListener('click', () => {
        lastOpenerCard = card as HTMLElement;
        openModal(card as HTMLElement);
      });
      card.addEventListener('keydown', (e) => {
        if ((e as KeyboardEvent).key === 'Enter' || (e as KeyboardEvent).key === ' ') {
          e.preventDefault();
          lastOpenerCard = card as HTMLElement;
          openModal(card as HTMLElement);
        }
      });
    });

    // Close
    closeBtn.addEventListener('click', closeModal);
    overlay.addEventListener('click', (e) => {
      if (e.target === overlay) closeModal();
    });
    document.addEventListener('keydown', (e) => {
      if (!overlay.classList.contains('active')) return;
      if (e.key === 'Escape') {
        closeModal();
        return;
      }
      if (e.key === 'Tab') {
        const focusable = Array.from(
          modal.querySelectorAll<HTMLElement>(
            'button:not([disabled]),a[href],input:not([disabled]),[tabindex]:not([tabindex="-1"])'
          )
        ).filter((el) => el.offsetParent !== null);
        if (focusable.length === 0) return;
        const first = focusable[0];
        const last = focusable[focusable.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    });
  </script>
</BaseLayout>
