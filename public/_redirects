# Redirects de entrega (P03) — somente canônico. Sintaxe: Workers Static Assets.
# Capacidade verificada na doc oficial em 2026-06-12 (_redirects nativo).
# Redirects de conteúdo (slugs legados nutrição-*) permanecem como meta-refresh
# do Astro até o P10 promovê-los a 301 reais aqui (PROM-06).
# HTTP→HTTPS é automático na zona Cloudflare (Always Use HTTPS — painel do dono).

# Canônico: apex → www (301)
https://somosecooa.com.br/* https://www.somosecooa.com.br/:splat 301
